<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quantum.mig.repo.MigrationAuditRepository">
	<insert id="record">
		INSERT INTO MIG_AUDIT
			( <include refid="mig.audit.col"/> )
		VALUES
			( <include refid="mig.audit.val"/> )
	</insert>
</mapper>